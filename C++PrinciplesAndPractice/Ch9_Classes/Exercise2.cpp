#include "../Libraries/std_lib_facilities.h"

class Name_pairs
{
    vector<string> name;
    vector<double> age;

    void read_names(vector<string> names);
    void read_ages(vector<double> ages);

    void print(const Name_pairs& pairs);
    void sort(Name_pairs& pairs);
};

int main()
{

    return 0;
}

void Name_pairs::read_names(vector<string> names)
{

}

void Name_pairs::read_ages(vector<double> ages)
{

}

void Name_pairs::print(const Name_pairs& pairs)
{

}

void Name_pairs::sort(Name_pairs& pairs)
{
    
}